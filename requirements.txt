# torch / torchvision / torchaudio are intentionally NOT listed — the NGC base image
# (nvcr.io/nvidia/pytorch) ships optimized, ABI-matched builds. Reinstalling torch from a
# public wheel index breaks flash-attn and other prebuilt extensions.
#
# transformers is pinned: 5.x changed the model-loading API and broke the OPT benchmark.
# Detection uses torchvision (already present), so ultralytics/opencv are intentionally absent.
transformers==4.44.2
accelerate
pyyaml
pillow
numpy
